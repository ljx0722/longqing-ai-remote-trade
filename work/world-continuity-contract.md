# Latest user steering

The user explicitly asks to add major world cities and more goods and make ALL geography continuous in one 2.5D world, not just ports: oceans, islands, inland cities according to actual Earth. Root will integrate renderer/main and sim. Do not touch those files.

Inland catalog agent task: create src/inland-catalog.ts exporting additionalInlandCities with same structural shape as InlandCity (id,name,region,era,lon,lat,connections:string[]) and additionalRoadWaypoints Record<string,number[][]> where key is sorted ids joined ':'. Do NOT import dominion.ts (avoid cycle). Add ~100-120 important historical mainland/island inland cities across Europe, East/South/Central Asia, Africa, Americas. Read existing src/dominion.ts 32 entries and src/data.ts seaports; no duplicate ids OR duplicated geographic cities. Existing ports include inland navigable river cities, so don't duplicate these.

Real coordinates, approximate historical names and era windows before1750. Include notable capitals/trade centers missing: Beijing, Nanjing, Hangzhou only if not existing, Xi'an only if not Changan etc. For modern famous names use historical sites reasonably; avoid assigning post1750-founded cities to antiquity. Connect explicit existing ports, inland cities or other additions on the same landmass. No heuristic roads across ocean. Include path waypoints around bays/large lakes where needed. Ensure every node has a chain to a real seaport through cities available by its own era (can raise era to later connector if necessary). Keep long distance roads reasonable/split via intermediates. Add at least Africa/Asia/Europe/Americas coverage, islands connected only to local ports. Add work/inland-catalog-check.mjs validating counts/duplicate names/geo bounds/references/reachability/era windows. Do not modify dominion.ts; root integrates arrays. Send completion/API promptly.

Goods agent task: expand src/catalog/goods.ts by at least180 distinct named historical goods/materials and src/catalog/markets.ts only if needed, with meaningful inland production regional origin descriptions/family ids and actual existing export seaport availability. Current 1525 -> at least1705. Stay before1750 windows. No raw generic numbered products. Read existing style, avoid name/id duplication. Ensure all added goods have nonempty actual market sources; test catalog-check and catalog-events-check. Root updates docs and attaches inland-specific specialty goods to caravans later if appropriate. Do not edit main.ts/sim.ts/dominion.ts/renderer.ts or inland-catalog.ts.
